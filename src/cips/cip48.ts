import { Metadata } from '../types/types';

/** @internal */
export function handleCip48(json: JSON, cnft: Metadata) {
  // take json
  // check valid or throw error
  // return cnft with new details
}
